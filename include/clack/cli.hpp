#pragma once

namespace clack {
   /**
   * @brief A simple CLI class that represents a command line interface.
   */
   class cli {
      public:
         /**
            * @brief Constructs a new CLI object.
            */
         cli();

         /**
            * @brief Destroys the CLI object.
            */
         ~cli();
   };
} // namespace clack