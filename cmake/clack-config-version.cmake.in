set(PACKAGE_VERSION @clack_VERSION@)

if("${PACKAGE_FIND_VERSION_MAJOR}" EQUAL "@clack_VERSION_MAJOR@")
   if("${PACKAGE_FIND_VERSION_MINOR}" EQUAL "@clack_VERSION_MINOR@")
      set(PACKAGE_VERSION_MATCH TRUE)
   elseif("${PACKAGE_FIND_VERSION_MINOR}" LESS "@clack_VERSION_MINOR@")
      set(PACKAGE_VERSION_COMPATIBLE TRUE)
   else()
      set(PACKAGE_VERSION_UNSUITABLE TRUE)
   endif()
else()
   set(PACKAGE_VERSION_UNSUITABLE TRUE)
endif()
